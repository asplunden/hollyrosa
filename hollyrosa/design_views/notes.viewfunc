<Document u'_design/notes'@u'20-e52313dbdb4a31e47a8289c869dfbbb3' {u'language': u'javascript', u'views': {u'all_attachments_per_target': {u'map': u"function(doc) {\n    if (doc.type=='attachment') {emit(doc.target_id, 1); }}", u'reduce': u'function(keys, values, rereduce) { return sum(values);}'}, u'notes_for_list_bookings': {u'map': u"function(doc) {\n    if ((doc.type=='activity') || (doc.type=='activity_group')) { if(doc.booking_info_id != null) emit(doc._id, {_id:doc.booking_info_id}); }}"}, u'number_of_notes_per_target': {u'map': u"function(doc) {\n    if (doc.type=='note') {emit(doc.target_id, 1); }}", u'reduce': u'function(keys, values, rereduce) { return sum(values);}'}, u'notes_by_target': {u'map': u"function(doc) {\n    if (doc.type=='note' || doc.type=='attachment') {emit(doc.target_id, null); }}"}, u'notes_by_target_datesorted': {u'map': u"function(doc) {\n    if (doc.type=='note' || doc.type == 'attachment') {emit([doc.target_id, doc.timestamp], null); }}"}}}>