<Document u'_design/all_activities'@u'27-c3cdcf76783a36883835f9b0f2f3c939' {u'language': u'javascript', u'views': {u'erasure': {u'map': u"function(doc) { if(doc.type!='activity' && doc.type!='activity_group' && doc.type!='day_schema' && doc.type!='user' && doc.type!='visiting_group') { if (!doc.do_not_delete) {emit([doc._id, doc.title], null);}}\nif (doc.type=='visiting_group') { if(!doc.do_not_delete) { emit(doc._id, doc);}}\n\n}"}, u'all_activities': {u'map': u"function(doc) { if(doc.type=='activity') {\n  emit([doc.zorder, doc._id], null)}; \n\t}"}, u'scan_all': {u'map': u'function(doc) { emit([doc._id, doc.title], null);\n}'}, u'all_activity_groups_and_activity': {u'map': u"function(doc) { if(doc.type=='activity') {emit([doc.activity_group_id, doc._id], doc)};}"}, u'all_activity_groups': {u'map': u"function(doc) { if(doc.type=='activity_group') {emit(doc.zorder, doc)};}"}, u'activity_titles': {u'map': u"function(doc) { if(doc.type=='activity') {emit([doc._id, doc.title], null)};}"}}}>