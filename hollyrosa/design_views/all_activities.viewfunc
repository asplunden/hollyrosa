<Document u'_design/all_activities'@u'21-d27ab992257068caf8b2e09d4a5f5fd8' {u'language': u'javascript', u'views': {u'all_activities': {u'map': u"function(doc) { if(doc.type=='activity') {\n  emit([doc.zorder, doc._id], null)}; \n\t}"}, u'erasure': {u'map': u"function(doc) { if(doc.type!='activity' && doc.type!='activity_group' && doc.type!='day_schema' && doc.type!='user' && doc.type!='visiting_group') { if (!doc.do_not_delete) {emit([doc._id, doc.title], null);}}\nif (doc.type=='visiting_group') { if(!doc.do_not_delete) { emit(doc._id, doc);}}\n\n}"}, u'scan_all': {u'map': u'function(doc) { emit([doc._id, doc.title], null);\n}'}, u'activity_titles': {u'map': u"function(doc) { if(doc.type=='activity') {emit([doc._id, doc.title], null)};}"}, u'all_activity_groups': {u'map': u"function(doc) { if(doc.type=='activity_group') {emit(doc.zorder, doc)};}"}}}>