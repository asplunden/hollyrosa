<Document u'_design/day_schema'@u'19-05cbbd2462e30481f4f9c2aa196bc5c3' {u'language': u'javascript', u'views': {u'day_schema': {u'map': u"function(doc) { if(doc.type=='day_schema') {\n  emit(doc._id, doc)};\n}"}, u'slot_schema_of_activity': {u'map': u"function(doc) { \n  if(doc.type=='day_schema') {\n       for ( activity_row in doc.schema) {\n           var slot_row = doc.schema[activity_row];\n   emit([doc._id, activity_row], slot_row)  } } }"}, u'slot_map': {u'map': u"function(doc) { \n  if(doc.type=='day_schema') {\n       for ( activity_row in doc.schema) {\n           var slot_row = doc.schema[activity_row];\n           var len = slot_row.length;\n           for(var i=1; i<len; i++) {\n               slot=slot_row[i];\n               emit([slot.slot_id, doc._id], [activity_row, slot])  } } } }"}}}>