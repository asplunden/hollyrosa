{"history_by_booking_id": {"map": "function(doc) { if (doc.type=='booking_history')  emit(doc.booking_id, null); }"}, "history_by_username": {"map": "function(doc) {  if (doc.type=='booking_history') {\n                                    emit(doc.changed_by, null);\n                                 }\n                               }"}, "all_history_by_visiting_group": {"map": "function(doc) { if (doc.type=='booking_history') { if (doc.visiting_group_id != null) emit([doc.visiting_group_id, doc.tiemstamp], null); }; }"}, "all_history": {"map": "function(doc) {\n\t\tif (doc.type=='booking_history') { \n\t\t  emit([doc.timestamp, doc.booking_id, doc.changed_by], null); }; }"}}